[project]
name = "rain-to-road"
version = "0.1.0"
description = "Flask machine learning application on Vercel"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "flask>=3.0.0",
    "numpy>=1.26.0",
    "pandas>=2.1.0",
    "scikit-learn>=1.3.0",
]

[tool.vercel]
entrypoint = "RainToRoad:app"
